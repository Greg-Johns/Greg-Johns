import GalleryLayout from '../app/GalleryLayout'
import Gallery from '../app/Gallery'

## Web design

<time>Feb. 6th, 2023</time>

I should not put this up.

<Gallery images={
  [
    'ngs1',
    'ngs2',
  ]
} />

export const meta = {
  author: 'Greg Johns'
}

export default ({ children }) => <GalleryLayout meta={meta}>{children}</GalleryLayout>

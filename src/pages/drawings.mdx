import MarkdownLayout from '../app/MarkdownLayout'

## Drawings

Here's a cartoon I drew of Mr. Spock with duck feat that I thought was funny but looking at it now I think maybe I should have added a duck with boots to complete the joke.

<i>
  ![TeaParty](../../drawings/spock.jpg)
</i>

export const meta = {
  author: 'Greg Johns'
}

export default ({ children }) => <MarkdownLayout meta={meta}>{children}</MarkdownLayout>
